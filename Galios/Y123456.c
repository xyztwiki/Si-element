#define Y
void y123456(a,b,c,d,e,f){
    char y;
const y1 = a * y^5 + b * y^4 + c * y^3 + d * y^2 + e * y + f;
const y2 = a * y^4 + b * y^3 + c * y^2 + d * y + e * y + f * y^(-1);
const y3 = a * y^3 + b * y^2 + c * y^1 + d * y + e * y^(-1) + f^(-2);
const y4 = a * y^2 + b * y^1 + c * y + d * y^(-1) + e * (-2) + f ^ (-3);
const y5 = a * y + b *  y^(-1) + c * y^(-2) + d * y^(-3) + e * y^(-4) + f * y^(-5);
const y6 = a * y^(-1) + b * y^(-2) + c * y^(-3) + d * y^(-4) + e * y^(-5) + f * y^(-6);
}
void matriyY(y1,y2,y3,y4,y5,y6){
    int matriyY[] = {y1,y2,y3,y4,y5,y6};
}